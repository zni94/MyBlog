import Icon from "../../Icon";
import {IDE_Icons} from "../../../icons";

const DataGripIcon = () => {
    const onDoubleClick = (e) => {
    }

    return <Icon title={'DataGrip'} onDoubleClick={onDoubleClick} thumbnail={IDE_Icons.DataGrip}/>
}

export default DataGripIcon;